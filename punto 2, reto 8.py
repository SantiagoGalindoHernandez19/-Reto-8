
range(1,1000,2) #Establecemos rango y salto de numeros
for n in range (1, 1000, 2): # Variable (n) donde guardaremos los valores
    print(n) #Impresion listado

range(2,1001,2) #Establecemos rango y salto de numeros
for n in range (2, 1001, 2): # Variable (n) donde guardaremos los valores
    print(n)